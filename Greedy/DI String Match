vector<int> diStringMatch(string s) {
        vector<int> res;
        int len = s.size();
        int min = 0;
        int max = len;

        for(int i =0; i<s.size();i++){
            if(s[i] == 'I'){
                res.push_back(min);
                min++;
            }else{
                res.push_back(max);
                max--;
            }
        }
        res.push_back(min);
        return res;
    }
