#include <iostream>
using namespace std; 
int main() {
    int a = 10;
    while(a > 5) { // a is outter scope variable
        int a = 1;
        cout << a << " ";
        a--;
    }
}

o/p is infinite loop
